{
  "name": "odin-restaurant-page",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "webpack --config webpack.prod.js",
    "dev": "webpack serve --config webpack.dev.js",
    "preinit-deploy": "npm run build && git add dist && git commit -m 'deploying a new dist'",
    "init-deploy": "git subtree push --prefix dist origin gh-pages",
    "postinit-deploy": "rm -r dist",
    "predeploy": "npm run build && git add dist && git commit -m 'deploying a new dist'",
    "deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages",
    "postdeploy": "rm -r dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^7.1.2",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.0",
    "style-loader": "^4.0.0",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4",
    "webpack-merge": "^6.0.1"
  }
}
